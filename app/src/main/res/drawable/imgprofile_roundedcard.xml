<shape xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Light background (better UI) -->
    <solid android:color="@color/material_dynamic_tertiary90"/>

    <!-- Rounded corners -->
    <corners android:radius="16dp"/>

    <!-- Soft border -->
    <stroke
        android:width="1dp"
        android:color="#E0E0E0"/>

</shape>